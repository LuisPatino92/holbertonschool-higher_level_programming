-- This script creates a new table in the current BD
-- The database name should be especified in the calling of the script

CREATE TABLE IF NOT EXISTS first_table (
	id INTEGER,
	name VARCHAR(256)
);
